<Page
    x:Class="UtilitiesDocs.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:UtilitiesDocs.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:SettingsViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid Padding="24" Background="{ThemeResource LayerFillColorDefaultBrush}">
        <StackPanel Spacing="20" HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBlock x:Uid="Settings_Title" Text="Settings" Style="{StaticResource TitleTextBlockStyle}" />
            
            <StackPanel Spacing="10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <TextBlock x:Uid="Settings_Language" Text="App Language" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <ComboBox ItemsSource="{Binding Languages}" 
                          SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                          DisplayMemberPath="Name" 
                          Width="250" />
                <TextBlock x:Uid="Settings_RestartNote" Text="* Additional changes may require app restart." 
                           Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
