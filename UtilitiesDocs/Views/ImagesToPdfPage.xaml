<Page
    x:Class="UtilitiesDocs.Views.ImagesToPdfPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:UtilitiesDocs.ViewModels"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <Page.DataContext>
        <vm:ImagesToPdfViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
            <SymbolIcon Symbol="Pictures"/>
            <TextBlock Text="Images to PDF" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <CommandBar DefaultLabelPosition="Right" Background="Transparent" IsOpen="False">
                <AppBarButton Icon="Add" Label="Add Images" Click="OnAddFilesClicked"/>
                <AppBarButton Icon="Delete" Label="Clear All" Click="OnClearClicked"/>
            </CommandBar>
            
            <ListView Grid.Row="1" ItemsSource="{Binding Files}" SelectionMode="Single" CanReorderItems="True" AllowDrop="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <SymbolIcon Symbol="Pictures" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                <TextBlock Text="{Binding Path}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <SymbolIcon Symbol="List" Grid.Column="2" Opacity="0.4" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Grid.Row="1" Text="Drag items to reorder" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,4,12,0" Opacity="0.5" Style="{ThemeResource CaptionTextBlockStyle}"/>
            <TextBlock Grid.Row="1" Text="No images selected" HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Style="{ThemeResource BodyTextBlockStyle}" Opacity="{Binding Files.Count, Converter={StaticResource EmptyCountToOpacityConverter}}"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Vertical" Spacing="10" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Convert to PDF" Style="{StaticResource AccentButtonStyle}" Click="OnConvertClicked" IsEnabled="{Binding Files.Count}"/>
                <ProgressRing IsActive="{Binding IsBusy}"/>
            </StackPanel>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
        </StackPanel>
    </Grid>
</Page>
