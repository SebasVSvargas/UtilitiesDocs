<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="UtilitiesDocs.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UtilitiesDocs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="UtilitiesDocs">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <NavigationView x:Name="NavView" 
                    SelectionChanged="NavView_SelectionChanged"
                    Loaded="NavView_Loaded"
                    PaneDisplayMode="Auto"
                    IsSettingsVisible="True"
                    OpenPaneLength="250">
        <NavigationView.MenuItems>
            <NavigationViewItem x:Uid="NavMerge" Tag="Merge" Icon="Document" Content="Merge PDFs"/>
            <NavigationViewItem x:Uid="NavUnlock" Tag="Unlock" Icon="Permissions" Content="Remove Passwords"/>
            <NavigationViewItem x:Uid="NavImages" Tag="ImagesToPdf" Icon="Pictures" Content="Images to PDF"/>
            <NavigationViewItem x:Uid="NavRotate" Tag="Rotate" Icon="Rotate" Content="Rotate Pages"/>
            <NavigationViewItem x:Uid="NavClients" Tag="Clients" Icon="People" Content="Manage Clients"/>
        </NavigationView.MenuItems>
        <Frame x:Name="ContentFrame"/>
    </NavigationView>
</Window>
